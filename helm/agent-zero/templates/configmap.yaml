apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-zero.fullname" . }}-env
  labels:
    {{- include "agent-zero.labels" . | nindent 4 }}
data:
  WEB_UI_HOST: "0.0.0.0"
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
